[project]
name = "voiceagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[cartesia,deepgram,livekit,local,openai]>=0.0.102",
    "python-dotenv>=1.2.1",
]
